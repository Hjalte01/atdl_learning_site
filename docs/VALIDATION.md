# Validation — 2026-09-15

## DiffAtlas guide addition

- Final `npm run build`: passed, 23 routes, zero errors/warnings/hints.
- Seven local SVG diagrams loaded and each links to its full-size asset. Training and inference diagrams visually inspected at full size; desktop/mobile page captures and dark-mode lab reviewed.
- Chromium checks: no JavaScript errors; no horizontal overflow at 390px; equations rendered without KaTeX errors.
- Lab checked at ᾱ = 0, 0.50, 0.64, 1.00: toy pixel values 0.2000, 0.7071, 0.7600, 0.8000.
- All DiffAtlas local links, table-of-contents anchors, and eight card section anchors resolve. Correct and incorrect quiz feedback verified.
- Printable deck now contains 18 cards (10 Flux + 8 DiffAtlas). Topic 2 links to DiffAtlas.
- Local DiffAtlas PDF SHA-256 matches the supplied T2_4_DiffAtlas.pdf: `96b623485471742c79c730fb9c8a66a72b014fdae50f6169dac3574417c8947b`. Content and selected results checked against that arXiv v1 PDF.
- GPT.md and AGENTS.md document paper shorthand, source fidelity, visual authoring, architecture/math mapping, and verification.
- Scope: the requested first installment, Topic 2 paper 4, is complete. Other queued papers remain for future requests. No deployment performed. The lab demonstrates Eq. 5 on synthetic data, not a trained segmentation model.

## Earlier Flux validation


- `npm run build`: passed; 23 static pages, zero Astro diagnostics.
- `npm audit --omit=dev`: zero vulnerabilities.
- Chromium desktop (1440px) and mobile (390px) checks passed with no JavaScript errors.
- Verified six topic entries and six Topic 2 paper entries.
- All 23 generated pages and 263 internal link occurrences checked; linked HTML anchors exist.
- Four display equations rendered with KaTeX and no math errors.
- Scalar slider checked at both endpoints and midpoint.
- Correct and incorrect quiz feedback, next-card navigation, recall disclosure, and theme switching checked.
- Ten practice cards and ten printable cards; answer-key toggle checked.
- No horizontal page overflow on the mobile Flux page. Desktop and mobile screenshots visually reviewed.
- Local Flux PDF checksum matches the original course PDF.
- Resolved project directory is outside the study Obsidian vault.

Scope at the time of the earlier checks: only Flux had a completed learning guide. The six-topic structure and twelve known paper records are present; other explanations and Topics 3–6 course material are intentionally pending.


## Reading library and next-guide workflow — 2026-09-16

- `npm run build`: passed, 24 pages, zero errors/warnings/hints. No project dependencies or lockfiles changed; no Nix dependency hash update needed.
- `python3 scripts/test-materials.py`: passed isolated PDF hash/copy checks, nested topic numbering, repeated imports, manual metadata preservation, paper-before-overview priority, newest-topic selection, and linked ready-guide exclusion.
- Chromium at 1440px and 390px: library search, topic filter, core-material filter and empty results passed; no horizontal overflow or browser errors; theme toggle checked. All displayed PDF and guide links returned HTTP 200. Mobile dark-mode screenshot visually inspected.
- Running local preview verified HTTP 200 at `http://127.0.0.1:4322/atdl_learning_site/materials`. No deployment, queue restart, push, or nxb performed.
- Incomplete requirement: the study/ATDL vault is unavailable to this session. The old `/home/hjalte/Documents/study/ATDL` path does not exist; `/srv/obsidian-webdav` denies reads, and sudo is unavailable under the no-new-privileges constraint. Only the two existing PDFs could be included. The catalog and UI explicitly mark the inventory incomplete. Full-vault import and verification remain pending a readable source path; the tracked goal is not complete.
- Assumed priority: all papers first, descending topic and ascending reading order; then overviews, slides, and extras with the same topic ordering. Selection against the current incomplete inventory yields Topic 2, paper 1 (REPA). No new explanation was requested for this setup task.

## REPA guide and recovered vault inventory — 2026-09-16

- Completed one automatically selected guide: Topic 2 paper 1, REPA, at `/papers/t2-paper-1`. Source is the supplied 43-page ICLR 2025 conference PDF (creation metadata 25 March 2025; no arXiv revision inferred). Read method, experiments and appendices; visually compared the source results table. Local `/papers/repa.pdf` matches the supplied file by SHA-256.
- Added seven original SVG teaching diagrams with reproducible `scripts/repa-figures.py`, architecture-linked equations, a numerical loss example, prediction disclosures, evidence and limitations. No trained model or custom interactive lab is presented. Training iteration ratios are distinguished from wall-clock and inference speed; guided and unguided results remain separate.
- Appended eight REPA cards. Verified all 18 existing cards are semantically unchanged and all 26 cards appear in printable material. Chromium exercised both correct and incorrect feedback for each new card on separate page loads (answers intentionally lock after submission).
- Final `npm run build`: passed, 24 routes, zero errors/warnings/hints. `python3 scripts/test-materials.py`: passed. `git diff --check`: passed. No dependencies, lockfiles or Nix hashes changed.
- Chromium: guide and every linked local resource returned HTTP 200; all new heading/card anchors resolve; seven images loaded; no KaTeX errors or page JavaScript errors. No horizontal page overflow at 390, 768 or 1440 pixels. Desktop and mobile dark-mode screenshots visually inspected, along with all seven enlarged diagrams and math. Corrected an inference arrow that crossed its label, rebuilt, and visually rechecked. SVG text bounds checked. Figures link to full-size SVGs for enlargement.
- The vault at `/srv/obsidian-webdav/study/ATDL` is now readable, resolving the earlier missing-inventory limitation. Imported all 27 present PDFs, verified every source-relative path and both source/copy SHA-256 hashes. Reviewed paper identities, assigned the four slide decks to their topics, and classified the generative-model reading overview as an overview. General overview books remain unassigned; topic textbooks remain extra reading. Separate source copies of the same textbook are preserved. No Topic 3–6 readings are present. All 27 PDF URLs returned HTTP 200.
- Inventory is complete for this vault snapshot; only REPA was newly authored. Both its paper and catalog status are ready. `python3 scripts/materials.py next` now selects Topic 2 paper 2, Mean Flows.
- Running static preview: `http://127.0.0.1:4322/atdl_learning_site/papers/t2-paper-1`, verified HTTP 200. This is a host-local preview, not deployment. Production at the registered `/atdl/` route awaits queue review/commit/deployment. No Git push, deployment, queue restart or nxb performed.
- Inventory import adds large repository-owned PDF assets, including a 137,568,865-byte textbook. The original bytes are retained for source fidelity; this is material repository growth for review and a consideration for the separate GitHub publishing workflow.

## Mean Flows guide — 2026-09-16

- Completed one automatically selected guide: Topic 2 paper 2, Mean Flows, at `/papers/t2-paper-2`. Followed the supplied 23-page NeurIPS 2025 conference PDF (creation metadata 14 February 2026; no publication date/arXiv revision inferred). Read method, algorithms, experiments, and technical appendices; visually checked source Table 2. The guide notes the source's SiT prose/table discrepancy and uses Table 2's values.
- Added seven reproducible SVG teaching diagrams (`scripts/meanflows-figures.py`), architecture-linked equations, a scalar JVP/target/loss example, prediction disclosures, evidence and limitations. No trained model or custom control lab is presented. Distinguishes marginal trajectories from straight training interpolants, exact identity from the stochastic training target, one step from one NFE, and flow-model training from the pretrained VAE.
- Appended eight cards; verified all 26 prior cards are semantically unchanged. All 34 cards appear in printable material. Chromium exercised correct and incorrect feedback for every new card and resolved each card's section anchor.
- Final `npm run build`: passed, 24 routes, zero errors/warnings/hints. `python3 scripts/test-materials.py`: passed. `git diff --check`: passed. No dependencies or lockfiles changed; no Nix dependency hash changes required.
- Chromium: guide and all linked local resources HTTP 200; all heading/TOC anchors resolve; seven images load; no KaTeX errors or JavaScript errors; no horizontal page overflow at 390, 768, or 1440 pixels. Enlarged diagrams visually inspected and crowded labels corrected. Desktop, mobile dark mode, and math screenshots reviewed. Figures link directly to full-size SVGs.
- Local `/papers/meanflows.pdf` SHA-256 matches the vault source and catalog: `c0b560a099c17966bbb0bb293f6c8d295f4a5a5f47b5698b4f26bd2d0f793caa`. Existing portable material copy retained. Guide and catalog are ready; automatic selection now yields Topic 2 paper 3, DiME. Topic overview and README scope updated.
- Running host-local preview verified reachable (HTTP 200): `http://127.0.0.1:4322/atdl_learning_site/papers/t2-paper-2`. Deployment at the registered `/atdl/` service remains pending queue review/commit/deployment. No push, deployment, queue restart, or nxb performed.

## DiME guide — 2026-09-16

- Completed one automatically selected guide: Topic 2 paper 3, DiME, at `/papers/t2-paper-3`. Read the supplied 19-page ACCV 2022 paper (proceedings pages 858–876), verified its identity against the official CVF conference record, and visually checked Table 1. No exact publication day or arXiv revision is inferred from the supplied PDF.
- Added eight reproducible SVG teaching diagrams (`scripts/dime-figures.py`), architecture-linked equations, a worked scalar guidance/sampling example, prediction disclosures, results, ablations and limitations. Distinguishes fixed weights from input gradients, noisy outer states from clean inner rollouts, and the approximate gradient transfer from an exact rollout Jacobian. Explicitly notes the paper's inconsistent clean-image subscripts and Eq. 11 diversity normalization; does not silently correct or recompute reported results. No trained model or custom interactive lab is presented.
- Appended eight four-choice cards; all 34 prior cards remain semantically unchanged. Chromium exercised correct and incorrect feedback for each new card and checked its section anchor. All 42 cards appear in printable study material.
- Final `npm run build`: passed, 24 pages, zero errors/warnings/hints. `python3 scripts/test-materials.py`: passed. `git diff --check`: passed. No dependency, lockfile or Nix dependency hash changes.
- Chromium: guide and linked local resources returned HTTP 200; all guide/TOC/card anchors resolve; eight images load; no KaTeX errors or JavaScript errors. No horizontal page overflow at 390, 768 or 1440 pixels. Desktop, mobile dark mode, rendered equations and all eight enlarged diagrams visually inspected. SVG text bounds checked; all figures link to full-size assets.
- `/papers/dime.pdf`, the catalog's existing portable copy, and the vault source have identical SHA-256: `c5ebf7da74d2b0ee4a19b876a57372dd8855bcd2da97db6998205a0cb41003fb`. Original portable material copy retained. Guide/catalog marked ready; topic overview and README updated. Automatic selection now yields Topic 2 paper 6, Qwen-Image.
- Changed the shared source-button label from “Paper on arXiv” to “Original paper” so the official conference link is labeled accurately.
- Running host-local static preview verified reachable (HTTP 200): `http://127.0.0.1:4322/atdl_learning_site/papers/t2-paper-3`. This is a preview of the built guide, not production deployment. Registered `/atdl/` deployment remains pending the queue's review/commit/deployment flow. No Git push, deployment, queue restart or nxb performed.

## Qwen-Image guide — 2026-09-17

- Completed one automatically selected guide: Topic 2 paper 6, Qwen-Image, at `/papers/t2-paper-6`. Used the supplied 46-page arXiv 2508.02324v1 report (arXiv stamp 4 August 2025; cover date 5 August). Verified identity against the version-specific arXiv record; read architecture, data, training, and evaluation sections and visually checked source Tables 8–9.
- Added ten reproducible SVG diagrams (`scripts/qwen-image-figures.py`), tensor dimensions, architecture-linked flow and preference equations, numerical examples, prediction disclosures, a reading map, and eight study cards. Clarifies the paper's noise-at-time-zero convention, dual reference conditioning, VAE adaptation, the limited I2I recipe disclosure, and historical evidence. Teaching Euler steps are not a production sampler; no model or custom interactive lab runs on this page.
- All 42 existing cards remain semantically unchanged; all 50 Topic 2 cards appear in printable study material. Chromium exercised correct and incorrect feedback for each of the eight new cards and resolved every card anchor.
- `npm run build`: passed, 24 pages, zero errors/warnings/hints. `python3 scripts/test-materials.py`: passed. `git diff --check`: passed. No dependencies, lockfiles, or required Nix dependency hashes changed.
- Chromium: guide and linked local resources returned HTTP 200; heading/TOC/card anchors resolve; ten figures loaded; no KaTeX errors or page JavaScript errors. No horizontal page overflow at 390, 768, or 1440 pixels. Reviewed desktop, mobile dark mode, rendered math, the diagram contact sheet, and the enlarged editing diagram. Shortened crowded diagram labels. SVG text bounds checked; all figures link to full-size SVGs. There are no new slider endpoints to test.
- `/papers/qwen-image.pdf`, its existing portable catalog copy, and the vault source have matching SHA-256 `16538964f7c446ec1fe422d0c7293b7ff7d7f009bc3788a116b20456607779bf`. Original source/copy retained. Guide and catalog marked ready; topic overview and README updated. All six Topic 2 guides are complete; automatic selection now yields Topic 1 paper 1. No other guide was authored.
- Running host-local preview verified reachable: `http://127.0.0.1:4322/atdl_learning_site/papers/t2-paper-6` (HTTP 200). Registered `/atdl/` production deployment remains pending the queue's commit/deployment flow. No Git push, deployment, queue restart, or nxb performed.
- Environment note: the normal filesystem/command sandbox failed to create its network socket. Authorized shell operations used escalation; visual review used browser screenshots read through the same route. No system configuration changes were needed.

## Information Bottleneck guide — 2026-09-17

- Completed one automatically selected guide: Topic 1 paper 1 at `/papers/t1-paper-1`. Read the supplied 19-page arXiv 1703.00810v3 (29 April 2017), verified identity against the version-specific arXiv record, and visually checked Figure 4's gradient curves and caption. Source architecture descriptions vary by experiment; §3.2 says arctan while §3.1 says tanh. The guide explicitly records both ambiguities.
- Added seven reproducible SVG teaching diagrams (`scripts/information-bottleneck-figures.py`), architecture-linked equations, exact two-bit and quantization examples, a numerical IB encoder assignment, SGD drift/noise examples, prediction disclosures, a source map and eight cards. Separates cross-entropy training, quantized information measurement and theoretical IB optimization. Reported depth milestones are not presented as matched-threshold or wall-clock speedups. No custom interactive lab or trained model is used.
- Chromium verified guide/local-resource HTTP 200 responses, heading/TOC/card anchors, seven loaded images, absence of KaTeX and JavaScript errors, both correct and incorrect feedback for every new card, and all 58 cards in printable study material. All 50 pre-existing Topic 2 cards remain semantically unchanged. No horizontal page overflow at 390, 768 or 1440 pixels; wide equations use the existing internal math scrolling.
- Visually reviewed desktop, mobile dark mode, rendered math, the seven-diagram contact sheet and source Figure 4. Corrected an architecture arrow crossing its caption and regenerated the figures. SVG text bounds checked; figures link to their full-size SVGs for enlargement. No new slider endpoints apply.
- `npm run build`: passed, 24 pages, zero errors/warnings/hints. `python3 scripts/test-materials.py`: passed. `git diff --check`: passed. No dependency, lockfile or Nix dependency hash changes.
- New `/papers/information-bottleneck.pdf`, existing catalog copy and vault original match SHA-256 `1dc4501aa1e4e75bda26e2679553a8df9b736d65ac0b848a5b0263f7141b5ba3`. Existing copies preserved. Guide and catalog marked ready; Topic 1 overview and README updated. Automatic selection now yields Topic 1 paper 2.
- Running host-local preview verified reachable (HTTP 200): `http://127.0.0.1:4322/atdl_learning_site/papers/t1-paper-1`. Registered `/atdl/` deployment remains pending the queue's commit/deployment flow. No Git push, deployment, queue restart or nxb performed.
- Environment: normal shell and image-read sandbox failed to create its network socket. Authorized shell operations and screenshot reads used escalation; no system changes were needed. A screenshot-harness CSS selector for a numbered heading was corrected; this was not a site defect.

## Nonvacuous Generalization Bounds guide — 2026-09-17

- Completed one automatically selected guide: Topic 1 paper 2 at `/papers/t1-paper-2`. Read the supplied arXiv 1703.11008v2 (19 October 2017), verified the version-specific arXiv identity, and visually inspected the source loss definition and Table 1. Explicitly records the printed 0–1 indicator typo and Algorithm 1 / §4.3 variance-initialization mismatch. Separates the random-label square-root diagnostic from inverse-KL probabilities.
- Added eight reproducible SVG diagrams (`scripts/nonvacuous-bounds-figures.py`), architecture-linked equations, computed scalar loss/KL/gradient and nested-certification examples, prediction disclosures, a reading map, and eight recall cards. Distinguishes deterministic training, posterior optimization, final certification and stochastic inference. No custom interactive lab or trained model runs on this page.
- Final `npm run build`: passed, 24 pages, zero errors/warnings/hints. `python3 scripts/test-materials.py`: passed. Existing 58 cards are semantically unchanged; all 66 cards appear in printable material. No dependencies or lockfiles changed; no Nix dependency hash updates required.
- Chromium verified guide and local-resource HTTP 200 responses, heading/TOC/card anchors, eight loaded images, no KaTeX or JavaScript errors, correct and incorrect feedback for every new card, and printable inclusion. No horizontal page overflow at 390, 768 or 1440 pixels. Visually inspected desktop, mobile dark mode, rendered equations and all eight diagrams; shortened crowded labels and rebuilt. Figures link to full-size SVGs. No new slider endpoints apply.
- New `/papers/nonvacuous-bounds.pdf`, the existing catalog copy and vault original have identical SHA-256 `4d28d0e2f5f7b841a5c79c0ecfa675cb6357323a008fa048dff9a07594ee6a35`. Source assets preserved. Guide/catalog marked ready; Topic 1 overview and README updated. Automatic selection now yields Topic 1 paper 3; no second guide was authored.
- Running host-local preview verified reachable: `http://127.0.0.1:4322/atdl_learning_site/papers/t1-paper-2` (HTTP 200). Registered `/atdl/` deployment remains pending the queue's commit/deployment flow. No Git push, deployment, queue restart or nxb performed.
- Environment: normal shell sandbox could not initialize its network socket; authorized commands used escalation. An automatic approval review usage-limit rejection interrupted browser verification; the user resumed the task and those checks subsequently passed. An initial preview request during the build returned 404; post-build browser checks returned 200.

## Bayesian Perspective on SGD guide — 2026-09-17

- Completed one automatically selected guide: Topic 1 paper 3 at `/papers/t1-paper-3`. Read the supplied 13-page ICLR 2018 conference PDF (creation metadata 14 February 2018; no arXiv revision inferred), including appendices. Visually inspected source Figure 6 and the noise-scale derivation. Records the introductory learning-rate/data-size footnote discrepancy and Appendix C figure-reference typo; follows the derived equations.
- Added seven reproducible SVG diagrams (`scripts/bayesian-sgd-figures.py`), architecture-linked equations, computed evidence and scalar-update examples, finite-batch endpoint calculations, prediction disclosures, evidence protocol panel and reading map. Distinguishes exact evidence from Laplace approximation, SGD from exact posterior sampling, and measured source claims from toy arithmetic. No custom interactive lab or trained model; no slider endpoint checks apply. Result curves remain linked in the supplied PDF rather than being assigned invented precision.
- Final `npm run build`: passed, 24 pages, zero errors/warnings/hints. `python3 scripts/test-materials.py` and `git diff --check`: passed. All existing cards preserved semantically; eight new cards bring printable total to 74. No dependencies or lockfiles changed; no Nix dependency hash updates required.
- Chromium verified HTTP 200 for the guide and its local resources, heading/TOC/card anchors, seven loaded figures, no KaTeX or JavaScript errors, correct and incorrect feedback for all eight new cards, and printable inclusion. No horizontal page overflow at 390, 768 or 1440 pixels. Visually inspected desktop, mobile dark mode, rendered math and all seven diagrams. Shortened two crowded labels, rebuilt and visually rechecked; every figure links to its full-size SVG.
- New `/papers/bayesian-sgd.pdf`, catalog copy and vault source share SHA-256 `b2f561a5e1073c4f05a22edf0803553bba561cd2864f65c2268b422755e0ae47`. Guide/catalog marked ready; Topic 1 overview and README updated. Automatic selection now yields Topic 1 paper 4; no second guide authored.
- Running host-local preview verified reachable (HTTP 200): `http://127.0.0.1:4322/atdl_learning_site/papers/t1-paper-3`. Registered `/atdl/` deployment remains pending the queue's commit/deployment flow. No Git push, deployment, queue restart or nxb performed.
- Environment: ordinary shell and image sandbox initialization failed on a network socket; authorized commands and screenshot reads used escalation. Existing Poppler and browser tools were reused without installing dependencies or changing system configuration.

## Deep Double Descent guide — 2026-09-17

- Completed one automatically selected guide: Topic 1 paper 4, `/papers/t1-paper-4`. Read the supplied 32-page J. Stat. Mech. 2021 article 124003, published 29 December 2021, including method, experiments and appendices; confirmed journal identity against a university-hosted copy. This is the updated journal version, not an assumed arXiv revision. August 2026 PDF metadata is packaging information. Visually inspected source Figure 3 and its caption.
- Added eight reproducible SVG teaching diagrams, a reproducible crop of source Figure 3, architecture-linked equations, numerical classifier-gradient and noise-metric examples, prediction disclosures, evidence and source maps. Generator: `scripts/double-descent-figures.py` (Python plus Poppler `pdftoppm`). No custom interactive lab or trained model; no slider endpoints apply.
- Records source inconsistencies: cross-entropy axis versus perplexity caption; 80k Transformer updates in main text versus 80 in Appendix B.1; 500k epochs versus the standard gradient-step recipe for the CNN sample comparison; translation-direction shorthand; categorical early-stopping appendix heading versus the explicit Figure 19 exception. Does not silently convert graph values or claim exact replication budgets. EMC and the critical interval remain an informal hypothesis, with toy examples distinguished from measured evidence.
- Appended eight four-choice cards. All 24 earlier Topic 1 cards remain semantically unchanged; Topic 2 cards were not edited. Chromium verified all 82 cards in printable material, correct and incorrect feedback for each of the eight new cards, and each new card's heading anchor.
- Final `npm run build`: passed, 24 pages, zero errors/warnings/hints. `python3 scripts/test-materials.py` and `git diff --check`: passed. No dependencies or lockfiles changed; no Nix dependency hash updates required.
- Chromium verified guide and linked local resources HTTP 200, heading/TOC/card anchors, nine decoded images, and no KaTeX errors. Checked page JavaScript errors. No horizontal page overflow at 390, 768 or 1440 pixels after adding a shared `.prose img` max-width/auto-height rule: the newly extracted raster figure exposed missing responsive sizing. Existing internal scrolling handles wide equations and tables. All nine figures link to full-size assets.
- Visually inspected desktop, mobile dark mode, worked-example layout, the rendered gradient matrix, all eight SVG diagrams in a contact sheet, and the extracted source plot. SVG text bounds passed. The contact-sheet harness initially used an SVG document context and then blocked remote image loading; switched its temporary contact sheet to embedded SVG data, without changing website behavior.
- `/papers/deep-double-descent.pdf`, the existing catalog copy, and vault original share SHA-256 `8f731731ecb6dcf08cb9736e55b6f84a39d50546db0d9e724e1a68dc34464e68`. Source assets preserved. Guide/catalog marked ready; Topic 1 overview and README updated. Next selection is Topic 1 paper 5; no second guide authored.
- Running host-local preview verified reachable (HTTP 200): `http://127.0.0.1:4322/atdl_learning_site/papers/t1-paper-4`. Registered `/atdl/` deployment remains pending queue commit/deployment. No Git push, deployment, queue restart or nxb performed.
- Environment: ordinary command/image sandbox initialization failed on a network socket; authorized shell and image reads used escalation. Temporary Nix shells provided Poppler and Chromium for verification; no system or repository dependency configuration changed.

## Understanding Grokking guide — 2026-09-17

- Completed one automatically selected guide: Topic 1 paper 5, `/papers/t1-paper-5`. Read the supplied 13-page NeurIPS 2022 conference paper and verified identity against the official proceedings record. The supplied PDF ends with the checklist; referenced Appendices A–K are absent. No arXiv revision, missing architecture details, regression accuracy rule, or exact appendix phase thresholds were invented.
- Added seven reproducible SVG teaching diagrams, an extracted source Figure 4, architecture-linked equations, worked embedding-transfer and gradient examples, prediction disclosures, source/evidence maps and eight cards. Generator: `scripts/grokking-figures.py` (Python plus Poppler for the crop). No custom interactive lab or trained model; no slider endpoints apply.
- Explicit source caveats: trivial self-comparisons in the printed RQI definition conflict with a literal zero random baseline; Proposition 1's proof needs fitted examples; normalization uses a variance-like expression without a square root; the quotient gradient includes a radial term absent from the subsequent linear relaxation; Figure 7 uses epochs on its axis while the prose says steps. The spectral timescale is presented as qualitative, not exact Adam dynamics. Missing appendices are identified throughout.
- Final `npm run build`: passed, 24 pages, zero errors/warnings/hints. `python3 scripts/test-materials.py` and `git diff --check`: passed. All 32 prior Topic 1 cards preserved semantically; Topic 2 cards unchanged. All 90 cards verified in printable study material. No dependency or lockfile changes; no Nix dependency hash updates required.
- Chromium verified HTTP 200 for the guide and its linked local resources, heading/TOC/card anchors, eight decoded images, no KaTeX or page JavaScript errors, and correct/incorrect feedback for all eight new cards. No horizontal page overflow at 390, 768 or 1440 pixels. Visually reviewed desktop, mobile dark mode, rendered math, all seven diagrams in a contact sheet, and the original source page/crop. Shortened crowded labels and rebuilt. SVG outer text bounds passed; all figures link to full-size assets.
- `/papers/understanding-grokking.pdf` and the existing portable catalog PDF both match catalog SHA-256 `99f55e2f3828894481b4c81e011ea81a7bed40506cf268cbf96dc5d46b0c78cd`. The repository-owned supplied PDF was the source used; no fresh vault-original comparison is claimed. Guide/catalog marked ready; Topic 1 overview and README updated. Next selection is Topic 1 paper 6; no second guide authored.
- Running host-local preview verified reachable (HTTP 200): `http://127.0.0.1:4322/atdl_learning_site/papers/t1-paper-5`. Production `/atdl/` deployment remains pending queue commit/deployment. No Git push, deployment, queue restart or nxb performed.
- Environment: the ordinary shell sandbox failed to initialize its network socket; authorized shell operations and screenshot reads used escalation. Existing browser dependencies and a temporary Poppler Nix shell were used without changing system configuration.
